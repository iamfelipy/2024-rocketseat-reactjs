import { styled } from '@/styles'

export const StarRatingContainer = styled('div', {
  display: 'flex',
  gap: '$1',
})

export const Star = styled('span', {
  color: '$purple100',
  fontSize: '$sm',
})
